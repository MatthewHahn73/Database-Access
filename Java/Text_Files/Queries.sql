create table employees (ID INT NOT NULL, 
						name VARCHAR(255), 
						address VARCHAR(255), 
						Primary Key(ID));

create table managers (ID INT NOT NULL,
					   name VARCHAR(255), 
					   address VARCHAR(255), 
					   Primary Key(ID));