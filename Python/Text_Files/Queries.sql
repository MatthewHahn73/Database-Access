create table dogs (ID INT NOT NULL, 
						name VARCHAR(255), 
						breed VARCHAR(255), 
						Primary Key(ID));

create table cats (ID INT NOT NULL,
					   name VARCHAR(255), 
					   breed VARCHAR(255), 
					   Primary Key(ID));